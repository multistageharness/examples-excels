{
  "name": "xlsx-month-diff-skills-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Unit tests that load the .agents/skills tree through the vendored GitHub Copilot SDK harness and confirm the model's output against goldens grounded in each SKILL.md.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "test:watch": "node --test --watch \"tests/*.test.mjs\"",
    "test:live": "COPILOT_LIVE=1 node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=20.6"
  },
  "license": "MIT"
}
